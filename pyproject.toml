[project]
name = "research-result-illustrator-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28,<1",
    "matplotlib>=3.10,<4",
    "numpy>=2,<3",
    "pillow>=11,<13",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pyyaml>=6,<7",
]
